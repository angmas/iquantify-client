<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation" data-toggle="collapse" data-target="#navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
        <li>{{#link-to "users"}}Users{{/link-to}}</li>
        <li>{{#link-to "researches"}}Researches{{/link-to}}</li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li>{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
        <li>{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
        {{!-- <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{email}} <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li class="change-password-nav"><a role="button" href="#change-password-modal" data-toggle="modal" data-target="#change-password-modal">Change Password</a></li>
            <li role="separator" class="divider"></li>
            <li class="sign-out-nav"><a href="#">Sign Out</a></li>
          </ul>
        </li> --}}
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<div class="jumbotron">
  <h1>iQuantify</h1>
  <p>Crowdsourced Research</p>
</div>
{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}
{{outlet}}
<div class="col-md-8 col-md-offset-2">

</div>
